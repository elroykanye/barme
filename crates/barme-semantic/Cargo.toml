[package]
name = "barme-semantic"
version = "0.0.0"
edition.workspace = true
license.workspace = true

[dependencies]
barme-core.workspace = true
# Vector index and embedding backends are still open (see ARCHITECTURE.md).
